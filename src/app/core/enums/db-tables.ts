// These are the table names of supabase database
export enum DbTables {
  QUOTATIONS = 'quotation_header',
  QUOTATION_ITEMS = 'quotation_items',
  PRODUCTS = 'products',
  CATEGORIES = 'categories',
  SCHOOL_GRADES = 'school_grades',
  SCHOOLS = 'schools',
}
