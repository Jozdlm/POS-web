export interface Category {
  id?: number;
  name: string;
  description: string;
  slug: string;
}

export interface CategoryDto {
  id?: number;
  name: string;
  description: string;
  slug: string;
}
